const {test} = require('@playwright/test');


test('Test-1', async () => {
})

test('Test-2', async () => {
    test.fail();
})

test('Test-3', async () => {
})

test('Test-4', async () => {
})

test('Test-5', async () => {
})
